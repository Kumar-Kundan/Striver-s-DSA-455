class Solution{
public:
	
	void printTriangle(int n) {
	    for(int i=0;i<n;++i){
	        for(int l=0;l<i;++l)
	        cout<<" ";
	        for(int j=n-i;j>0;--j)
	            cout<<"*";
	        for(int k=n-i-1;k>0;--k)
	            cout<<"*";
	        cout<<endl;
	    }
	}
};
